#include<iostream>
using namespace std;

int findDuplicate(int arr[], int size) {
    int index = 0;
    // int duplicate[index] = {0};
    int duplicate[100] = {0};

    int ans = 0;

    for(int i = 0; i < size; i++) {
        ans
    }
}